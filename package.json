{
  "private": true,
  "scripts": {
    "clean": "npm -C Backend run clean && npm -C Steav-Ticket run clean",
    "clean:deps": "rm -rf Backend/node_modules Steav-Ticket/node_modules",
    "clean:all": "npm run clean && npm run clean:deps",
    "install:all": "npm -C Backend install && npm -C Steav-Ticket install"
  },
  "dependencies": {
    "@nestjs/mapped-types": "*",
    "@nestjs/schedule": "^6.1.0",
    "axios": "^1.13.3",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.3"
  }
}
